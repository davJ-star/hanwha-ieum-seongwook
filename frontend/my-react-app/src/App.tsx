import * as React from 'react';
import Counter from './Counter';

const App = () => {
  return (
    <div>
      <h1>Vite + React + Redux + TypeScript Example</h1>
      <Counter />
    </div>
  );
};

export default App;
