/*postcss.config.mjs: PostCSS 설정 파일
* PostCSS 설정을 정의하고 프로젝트에 적용*/

/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
  },
};

export default config;
